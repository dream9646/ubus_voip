MKNAME:=$(lastword $(MAKEFILE_LIST))

TOOLPREFIX=/usr/bin/
# define START_CLI_IN_MAIN for x86 binary
PLATFORM_CFLAGS= -DX86 -DSTART_CLI_IN_MAIN
CHIPSET=x86

export TOOLPREFIX

$(info $(MKNAME) CHIPSET=$(CHIPSET))
$(info $(MKNAME) TOOLPREFIX=$(TOOLPREFIX))
#$(info $(MAKEFILE_LIST))
